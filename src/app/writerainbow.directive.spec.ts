import { WriterainbowDirective } from './writerainbow.directive';

describe('WriterainbowDirective', () => {
  it('should create an instance', () => {
    const directive = new WriterainbowDirective();
    expect(directive).toBeTruthy();
  });
});
